<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 全局setting配置，根据需要添加 -->
	<!-- <settings>
		<setting name="" value=""/>
	</settings> -->

	<!-- 配置别名 -->
	<typeAliases>
		<!-- 批量扫描别名 -->
		<package name="gao.ssm.po"/>
	</typeAliases>

	<!-- 配置mapper
	由于我们使用spring和mybatis的整合包进行mapper扫描，这里不需要配置了
	必须遵循：mapper.xml与mapper.java文件同名且在同一目录当中
	 -->
	<!-- <mappers>
		<package name="gao.ssm.mapper"/>
	</mappers> -->
</configuration>